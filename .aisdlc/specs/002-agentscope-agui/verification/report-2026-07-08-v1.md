# AgentScope AG-UI 前端历史回放验证报告 v1

## Status

验证未通过。AI-SDLC 结构、AC、代码证据和测试路径已闭合，但隔离 worktree 缺少前端依赖。

## Commands

- `pnpm build`: FAIL

## Smoke Path

1. 发起一次 AgentScope thinking 工具调用。
2. 确认工具卡显示在当前 assistant message 下。
3. 切换/刷新后重新打开同一会话。
4. 确认历史 assistant message 下恢复工具卡。
5. 打开普通聊天会话，确认无 `agentEvents` 时正常渲染。

## Evidence Mapping

- AC-AGW-001: `chatStore.requestChatList`, `foldAgentEventsToToolCalls`
- AC-AGW-002: `chatWithId/index.vue`
- AC-AGW-003: optional field guards
- AC-AGW-004: `pnpm build`

## Result

- Build failed before typecheck because `vue-tsc` was not found.
- pnpm warning: local `package.json` exists, but `node_modules` is missing.
- Minimum fix before re-run: execute `pnpm install` in this worktree, then re-run `pnpm build`.
